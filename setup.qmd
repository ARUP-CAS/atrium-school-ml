---
title: "Setup Guide"
date: 2026-09-03
---

::: {.callout-important}

## Alert!

The instructions might change, please check this page shortly before the school begins. 

:::


::: {.callout-tip}
## In a hurry? You can skip most of this

The notebooks run unchanged in [Google Colab](https://colab.research.google.com/){.external},
on the school JupyterHub, and on your own machine — see
[Materials](materials.qmd). If you only ever use Colab you need nothing on this
page except a Google account, and Claude Code if you want coding help. Installing
locally is still worth it: it is what you will use after the school.
:::


## Python

Install Python (3.10+), follow guidance on the official [website](https://www.python.org/){.external} or use your system's package manager.

<!-- ### JupyterLab

Install JupyterLab with `pip`:

```python
pip install jupyterlab
``` -->


## Git

Install Git, on Windows foolow this [guide](https://gitforwindows.org/){.external}, on MacOS this [one](https://git-scm.com/install/mac){.external} and on Linux you probably already have it and presumably know what you are doing.


## IDE

Install and use an IDE of your choice, preferably [Zed](https://zed.dev/){.external} or [VSCode](https://code.visualstudio.com/){.external}.


## Course materials and Python packages

Once Python and Git are in place, clone the course repository and install the
packages the notebooks need:

```bash
git clone --depth 1 https://github.com/ARUP-CAS/atrium-school-ml.git
cd atrium-school-ml
python -m venv .venv
```

Then, activate the environment (`.venv\Scripts\activate` on Windows,
`source .venv/bin/activate` on macOS and Linux) and install:

```bash
pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cpu --extra-index-url https://pypi.org/simple
```

::: {.callout-important}
## Use the CPU index for PyTorch

That `--index-url` matters. A plain `pip install torch` downloads the CUDA
runtime — around **2.5 GB** — even on a laptop with no NVIDIA graphics card.
With the CPU index the whole install finishes in well under a minute, and
everything we do this week should run on a CPU.
:::

::: {.callout-tip}
## Datasets come later

Do **not** try to download the case-study data now. It is not in the repository —
some of it is several gigabytes, and some cannot be redistributed. Each notebook
fetches what it needs, and caches it so it downloads only once.
:::

Each notebook's first cell then finds this repository around it and installs
nothing further.

If none of this works on your machine, do not worry — the same notebooks run in
Google Colab (one click, nothing to install) and on a JupyterHub we will give you
access to on the first day.


## Claude Code

::: {.justify}
During the school you will be granted access to [Claude Code](https://claude.com/product/claude-code) to support you in coding.
Before the school starts, please install **Claude Code** and configure the API key you will be given by the organisers, or if using Zed, add the API key there.
:::

::: {.callout-note}
If you already use a different coding assistant or have a Claude subscription, you can use your own settings, we do not require you to use this.
:::

### Install Claude Code

::: {.panel-tabset}
## Windows

Open PowerShell and run:

```powershell
irm https://claude.ai/install.ps1 | iex
```

Claude Code runs natively on Windows -- no WSL, Node.js, or npm required.

::: {.callout-note}
## Optional

Installing [Git for Windows](https://gitforwindows.org/){.external} gives Claude Code a Bash shell to use, which some participants find nicer to work in. It's entirely optional — without it, Claude Code falls back to PowerShell, which works fine.
:::

## macOS

Open Terminal and run:

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

## Linux

Open a terminal and run:

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

This also applies if you're using WSL (Windows Subsystem for Linux) — run this inside your WSL terminal.
:::

Once installed, Claude Code updates itself automatically — no need to repeat this step.  
Official install guide is [here](https://claude.com/product/claude-code).

### Add your API key

:::: {.panel-tabset}

## Claude settings file

*This method works the same way on every OS, and is the most reliable — it doesn't depend on which shell or terminal you use, and survives restarts.*

Create/edit a file at `~/.claude/settings.json` (on Windows: `%USERPROFILE%\.claude\settings.json`), creating the `.claude` folder first if it doesn't exist yet. Add this line in the `JSON` -- replace the placeholder with the key you were given:

```json
{ "env": {
    "ANTHROPIC_API_KEY": "sk-ant-api03-...",
    "ENABLE_PROMPT_CACHING_1H": "1"
} }
```

*Note that we also change caching to 1h, what saves a bit of tokens on the transfers.*

After the training school, simply delete this line from `.claude/settings.json`.

## Environment variable

If you'd rather set an environment variable instead:

::: {.panel-tabset}
## Windows

```powershell
setx ANTHROPIC_API_KEY "sk-ant-api03-REPLACE-ME"
```

::: {.callout-warning}
This command only takes effect in a **new** terminal window opened after you run it; your current window won't pick it up.
:::

*Note that env. variable written using `setx` is permanently saved.*

## macOS

```bash
export ANTHROPIC_API_KEY="sk-ant-api03-REPLACE-ME"
```

(This assumes the default `zsh` shell.)

Setting the env. var. this way is not permanent and you need to run this in each new terminal session.

## Linux

```bash
export ANTHROPIC_API_KEY="sk-ant-api03-REPLACE-ME"
```

(This assumes the default `bash` shell.)

Setting the env. var. this way is not permanent and you need to run this in each new terminal session.
:::

::::

### First run

Run in your terminal `claude`.

Claude Code will detect the key and ask you to approve it once -- choose **Yes**. This choice is remembered, so you won't be asked again.

::: {.callout-note}

## Verify it worked

Inside Claude Code, run `/status` — it should show that you're authenticating with the API key. If something still looks off, run `claude doctor` for a self-check.

:::


::: {.callout-important}
# Attention

- **Never** share your key or commit it to a repository.
- The key is time-limited and will stop working after the school.
- Only models **Claude Sonnet 5** and **Claude Haiku 4.x** are available to you.
:::


## Docker or Podman

Install Docker or Podman:

::: {.panel-tabset}
## Docker

Follow official documentation [here](https://docs.docker.com/get-started/get-docker/){.external}. On Windows, Docker is probably easier to install.

## Podman

Documentation [here](https://podman.io/){.external}.
:::


## Accounts

You might need a Google account (to access Google Colab) and a [HuggingFace](https://huggingface.co/){.external} account.